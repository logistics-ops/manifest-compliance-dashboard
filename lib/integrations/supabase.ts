import type { Carrier } from "@/types/carrier";

export async function listCarriers(): Promise<Carrier[]> {
  // Future Supabase integration point. Map database rows into the Carrier shape.
  return [];
}
